spring:
 datasource:
  url: jdbc:mariadb://localhost:3306/spring_boot
  username: root
  password: root
  driver-class-name: org.mariadb.jdbc.Driver

mybatis:
 type-aliases-package: com.example.demo.dto
 mapper-locations: mapper/*.xml

server:
 port: 9996
